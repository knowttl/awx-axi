{
  "$tag": "24.6.1",
  "$source": "awx/api/serializers.py:4567-4607",
  "$note": "Survey specification for a workflow template. Defines inputs for workflow execution.",
  "status": 200,
  "body": {
    "name": "Release pipeline survey",
    "description": "Release parameters",
    "spec": [
      {
        "question_name": "Release Version",
        "variable": "release_version",
        "type": "text",
        "required": true,
        "default": "1.0.0"
      }
    ]
  }
}
